<?xml version="1.0" encoding="utf-8"?>
<!-- The "rough calibration" / degraded-estimate warning banner — matches
     the sensor-calibration mockup's own info banner exactly:
     bg-orange-50 / border-orange-200/70. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/warning_container" />
    <corners android:radius="14dp" />
    <stroke android:width="1dp" android:color="#FED7AA" />
</shape>
